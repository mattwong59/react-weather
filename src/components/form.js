import React, { Component } from 'react';

class Form extends Component {
    render(){
        return(
            <React.Fragment>
                FORM 
            </React.Fragment>
        );
    }
};

export default Form;